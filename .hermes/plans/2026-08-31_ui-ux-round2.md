# UI/UX Polish Round 2 — 20 Item

Status: working tree bersih, verify hijau (28/28), base commit `3375797`.
Semua item = vanilla TS/CSS, glass tokens ada, lucide ada.

## A. Visual pass (CSS murni, cepat)
1. **Shadow panel berlapis**: `.glass` box-shadow ganda (`0 8px 24px` + `0 1px 2px` tipis) — floating feel Apple.
2. **Scrollbar tipis accent**: `#panel::-webkit-scrollbar` 3px, thumb accent 0.35 — sekarang default chunky.
3. **HUD corner controls** (kiri atas canvas): kumpulan tombol ghost glass — reset kamera (home), toggle grid, info — hover ring.
4. **Empty-state onboarding hint**: bubble glass kecil "Drag untuk orbit · scroll untuk zoom" fade-out 6s, hanya sekali (localStorage).
5. **Chart hover: bubble nilai** — bubble glass di atas crosshair chart menampilkan nilai V/M/y (angka di chart hilang per permintaan, hover kini satu-satunya sumber angka → wajib ada).
6. **Chip aktif state lebih tegas**: chip aktif = accent fill 20% + icon check — status ribbon on/off jelas.
7. **Slider value bubble selalu tampil nilai saat drag + unit di label row** — unit pindah ke bubble.
8. **Typografi headline**: judul panel 15px semibold + caption 11.5px muted; hasil utama δ/σ 26px; letter-spacing -0.02em.
9. **Transisi panel modul**: panel masuk translateY(8px)+fade 260ms spring saat ganti mode explore/explain (satu CSS class).
10. **Focus ring konsisten**: semua tombol UI focus-visible accent (sudah sebagian; lengkapi replay-btn, picker-btn... sudah, cek .picker-opt).
11. #11 **Timeline ticker** di bawah charts — "t = 0.0 s" tabular-nums, update saat replay only (satu div).
12. **Warna status**: .warn merah #ff453a sudah; tambah .good hijau #30d158 untuk SF ≥ 1.5.
13. **Toast posisi**: bottom 200px → bottom 168px (nempel timeline).
14. **Panel scroll fade mask** atas/bawah (mask-image linear) — kesan scrollable halus.

## B. Interaksi 3D↔UI (butuh TS)
15. **Reset view button** (item A3) — kamera balik ke fit awal (sm.resetView()).
16. **Crosshair 3D marker**: hover chart → garis vertikal + titik di beam 3D (garis + dot kecil accent, hide saat leave) — feedback dua arah §20 rule 2.
17. **Toggle ribbon V/M/y terpisah**: satu chip per fungsi (V | M | y) bukan satu chip semua — kontrol granular.
18. **Preset "skenario"**: chip preset sekarang juga set tema langit? TIDAK — scope freeze. Ganti: preset men-trigger replay animasi load ramp (settle terlihat) — feel hidup.
19. **Tooltip shortcut**: tombol HUD beri title (a11y).
20. **Performance guard**: pool chartData buffer —hindari alokasi array per frame saat drag (§15).

## Eksekusi
- Batch 1 (CSS): 1,2,6,7,8,11,12,13,14,19 — satu commit.
- Batch 2 (TS UI): 3,4,15,16,17,18,20 — dua commit.
- Verify per batch; komit tiap batch; HMR live.

## Verifikasi
- npm run verify hijau.
- Preview: hover chart → marker 3D; chip V/M/y terpisah; HUD kiri atas; reset view balik.
- 28/28 tests.

## Risiko
- Crosshair marker perlu koordinat beam → raycast tidak perlu: garis vertikal di x hover × scale — murah.
- Bubble nilai chart: reuse crosshair existing mini-chart, tambah <div> bubble HTML di host chart? TIDAK — bubble = SVG <text> inline style (aturan SVG no var()).
