# Master Prompt v3 — Modul Balok: Gap Analysis & Langkah Lanjutan

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** Memetakan sisa pekerjaan modul balok terhadap master prompt v3, urut prioritas.

**Architecture:** Tidak ada perubahan solver (piecewise exact, 31 tests). Fokus: melengkapi klaim
master prompt yang belum tuntas di Lab Balok, lalu modul berikutnya.

**Tech Stack:** Three.js sweep profil (sudah ada), solver existing, UI tokens ada.

---

## Status modul balok vs master prompt v3

| Klaim v3 | Status | Bukti |
|---|---|---|
| Profil asli tereksrusi (IPE, H, rect, circular) | ⚠️ Sebagian | `profilePoints()` handle `rect`/`circular`/I-shape via section presets (IPE300, WF400, rect300×600) — **circular ada di engine, belum ada di picker UI** |
| Ribbon 3D V/M/y morph live | ✅ | `beam-3d.ts:115`, label `CanvasLabel` |
| Load ramp animasi | ✅ | spring-settle ada (k=170 c=26) |
| Diagram 2D companion live-sync | ✅ | mini-chart 3 strip |
| Camera orbit + spring | ✅ | fitTo 1.45× |
| Drag beban 3D (mode Titik) | ✅ bonus | dragProxy r0.28 |
| Load vectors 3D pulse | ⚠️ | panah statis; "pulse halus" belum |
| Kurva elastis y(x) di 3D | ❌ | hanya di ribbon mode y; bukan kurva terpisah sepanjang beam |

## Backlog balok (urut nilai edukasi ÷ biaya)

1. **Circular hollow section di picker** (P0, ~20 min) — engine `profilePoints` sudah support
   `circular`; tinggal tambah preset `CHS 219×8` ke `section.ts` + label UI. Nilai: klaim v3
   "IPE, H, rect, circular" jadi benar; rumus I = π(D⁴-d⁴)/64 masuk daftar penampang.
2. **Kurva elastis y(x) overlay 3D** (P1, ~45 min) — garis melengkung mengikuti beam (offset
   dari solver `y(x)`), warna accent; master prompt §64 pipeline terlihat utuh di 3D.
3. **Pulse halus load vectors** (P2, ~15 min) — opacity sin(t) di material panah; "teranimasi
   pulse halus" v3 §117.
4. **CHS + HSS di calc info** (P2, ~5 min) — detail I,Z tip di panel saat penampang circular.

## Diluar balok — modul berikutnya (jangan sekarang)

- Level 4 FEM 3D (frame solver, 6 DOF) — besar, modul sendiri
- Level 5 dinamika (gempa/angin) — besar
- Level 6 lab 3×2×4 — besar

## Rencana implementasi (kalau disetujui — Task bite-size)

### Task 1: CHS 219×8 di picker penampang
- Modify `src/structural/models/section.ts`: preset `{ id:'chs219', label:'Baja tubular Ø219×8',
  shape:'circular', dims:{d:219,t:8}, materialId:'steel', Iy: 1.9929e8, Zy:..., mass: 41.6 }`
  (angka dihitung & dicek ulang saat eksekusi; I dalam mm⁴ konsisten preset lain)
- Modify `src/ui/panels/beam-panel.ts` — SECTION sudah baca dari presets, biasanya nol perubahan UI
- Verify: pilih CHS → beam 3D jadi tabung; δ SS UDL dekat closed-form π⁴… gunakan benchmark
  existing test tolerance 0.1%
- Commit: `feat: penampang tubular CHS 219x8 (circular engine sudah ada)`

### Task 2: Kurva elastis 3D y(x)
- Modify `src/visualization/three/beam-3d.ts`: line/spline 33 titik dari solver deflection array
  (sudah dikembalikan solver), offset y·deformScale, warna accent, `setSpan` ikut
- Verify: mode M, ubah beban → kurva ikut; 60 fps
- Commit: `feat: kurva elastis y(x) overlay 3D`

### Task 3: Pulse load arrows
- Modify `beam-3d.ts` material panah: `opacity = 0.75 + 0.2·sin(t·2π/1.6)`
- Verify: 60 fps, tak mengganggu
- Commit: `feat: pulse halus panah beban`

### Task 4 (opsional): detail penampang circular di panel
- Modify `beam-panel.ts`: info Iy/Zy/r via fmtSci
- Commit: `feat: info penampang CHS di panel`

## Validation
- `npx tsc --noEmit` clean per task
- `npm run verify` 31/31 tetap
- QA visual Chrome dark+dusk: pilih CHS → tabung; kurva y(x) menempel beam; panah berdenyut
- Push → Pages auto-deploy (workflow sudah ada)

## Risks
- Nilai Iy CHS harus dihitung benar (π(D⁴−d⁴)/64) — verifikasi manual sebelum commit
- Kurva y(x): deformasi scale jangkar 100 kN — jaga konsistensi dengan warp beam existing
  (beam sudah terdeformasi spring; kurva harus pakai offset yang sama, bukan dobel)
- Jangan sentuh solver (guard project)

## Open questions
- Kurva y(x) di atas atau di bawah beam? (usulan: bawah, arah lendutan sebenarnya)
- CHS pakai baja S235 sama seperti IPE?
