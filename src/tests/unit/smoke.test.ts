import { describe, it, expect } from 'vitest';

// ponytail: placeholder bukti harness — diganti test riil di PHASE 1.
describe('scaffold', () => {
  it('verify harness berjalan', () => {
    expect(2 + 2).toBe(4);
  });
});
